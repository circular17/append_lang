{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build append grammar",
            "type": "shell",
            "command": "./build-grammar.sh"
        },
        {
            "label": "build",
            "dependsOn": ["build append grammar"]
        }
    ]
}